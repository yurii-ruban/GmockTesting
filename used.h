#ifndef USED_H
#define USED_H
#include <string>

using namespace std;

class Used
{
public:
    virtual string concat(string first, string second);
    virtual string copy(string str);
    virtual string revers(string str);
};

#endif // USED_H
